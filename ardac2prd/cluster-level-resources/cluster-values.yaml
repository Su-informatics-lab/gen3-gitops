cluster: "ardac2prd"

accountNumber: "742378880825"

configuration:
  configurationRepo: https://github.com/Su-informatics-lab/gen3-gitops.git
  configurationRevision: main

alb-controller:
  enabled: true
  configuration:
    enabled: false

aws-s3-mountpoint:
  enabled: true
  configuration:
    enabled: false

calico:
  enabled: true
  configuration:
    enabled: false

cert-manager:
  enabled: true

coreDNS:
  enabled: true
  configuration:
    enabled: false

ebs-csi-driver:
  enabled: true
  configuration:
    enabled: false

external-secrets:
  enabled: true

fluentd:
  enabled: false
  configuration:
    enabled: false

kube-state-metrics:
  enabled: true
  configuration:
    enabled: false

grafana-alloy:
  enabled: false
  configuration:
    enabled: false

karpenter:
  enabled: true
  targetRevision: "1.14.0"
  configuration:
    enabled: true

karpenter-crds:
  amiSelectorName: "amazon-eks-node-al2023-x86_64-standard-1.36-*"
  amiOwner: "amazon"
  enabled: true
  usaAlias: true # Temporary workaround for an upstream chart/template typo; remove after upgrading to a chart version that correctly uses `useAlias` only.
  useAlias: true
  alias: "al2023@v20260724"
  selectorTag: ardac2prd
  migration: true
  default:
    consolidationPolicy: "WhenEmpty"
    consolidateAfter: "10m"
    volumeSize: 30Gi
    requirements:
      - key: karpenter.sh/capacity-type
        operator: In
        values:
          - spot
      - key: kubernetes.io/arch
        operator: In
        values:
          - amd64
      - key: karpenter.k8s.aws/instance-category
        operator: In
        values:
          - r
          - c
          - m
          - t
      - key: karpenter.k8s.aws/instance-size
        operator: NotIn
        values:
          - nano
          - micro
          - small
          - medium
  jupyter:
    consolidationPolicy: "WhenEmptyOrUnderutilized"
    consolidateAfter: "10m"
  workflow:
    consolidationPolicy: "WhenEmptyOrUnderutilized"
    consolidateAfter: "10m"

vpc-cni:
  enabled: true
  configuration:
    enabled: false
