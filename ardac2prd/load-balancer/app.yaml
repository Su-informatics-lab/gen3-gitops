apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: load-balancer-ardac2prd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Su-informatics-lab/gen3-gitops.git'
    targetRevision: main
    path: ardac2prd/load-balancer/manifests
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
